{
  "name": "@z-ai/glm-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use Z.ai GLM Coding Plan models from Claude Code.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node --check plugins/glm/scripts/glm-companion.mjs && node --test tests/*.test.mjs"
  }
}
